use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "quality-local-only"

search:
  default_lang: "en"
  formats:
    - json

outgoing:
  request_timeout: 8.0
  max_request_timeout: 10.0

botdetection:
  ip_limit: 0
  link_token: false
