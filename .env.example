# Required. Generate independent values with: openssl rand -hex 32
SEARXNG_SECRET=

# Optional host ports.
SEARXNG_PORT=8888

# Optional IA_Recuerdo integration. Leave both empty to disable it.
MEMORY_URL=
MEMORY_APIKEY=
SOURCERUDDER_AUTH_KEY=
SOURCERUDDER_PORT=8080
